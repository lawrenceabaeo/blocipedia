<banner class="pure-u-1-1"> 
  <h1 class="banner-head">Blocipedia: Social, Markdown Wikis</h1>
  <div class="banner-buddy"><p>Create and share Markdown Wikis</p>
    <% if current_user %>    
      <%= link_to "My wikis", wikis_path, class: 'pure-button pure-button'  %>
    <% else %>
      <%= link_to "Sign Up", new_user_registration_path, class: 'pure-button pure-button-primary'  %>
    <% end %>
  </div>
</banner>

<div class="l-content">
  <div class="pricing-tables pure-g-r">
    
    <% if !current_user %>
      <div class="pure-u-1-2">
        <h2>Create as many public wikis as you want!</h2>
        <%= link_to "Free Plan", new_user_registration_path, class: 'pure-button' %>
      </div>
      <div class="pure-u-1-2">
        <h2>Create private wikis!</h2>
        <%= link_to "Premium Plan", new_subscription_path, class: 'pure-button' %>
      </div>
      <div class="pure-u-1-3">
      </div>
      <div class="pure-u-1-3 sign-up-later-area">
        <h4>No thanks, I'll sign up later</h4>
        <%= link_to "Explore wikis", wikis_path %>
      </div>
      <div class="pure-u-1-3">
      </div>
    <% else %>
      <% if (Subscription.find_by_user_id(current_user))%>
        <div class="pure-u-1-1">
          <h2>Current Status: Premium</h2>
          <p>Thanks for being a premium member!</p>
        </div>
      <% else %>
        <div class="pure-u-1-1">
          <h2>Create private wikis!</h2>
          <%= link_to "Premium Plan", new_subscription_path, class: 'pure-button' %>
        </div>
      <% end %>
    <% end %>

  </div>

</div>
